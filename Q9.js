// write a function it takes array as an input and return average of those  numbers which are in array  


function avgNumbers(a){
      let num = 0
      let avg ;
      for(let i=0;i<a.length;i++){
          num = num + a[i]
      }
  
      return  avg = (num/a.length)
    
}

console.log(avgNumbers([10,3,5,6,1]))






